[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --chip nRF5340_xxAA"

[build]
target = "thumbv8m.main-none-eabi"
rustflags = [
    "-W", "linker-messages",
]
target-dir = "../build/target-net"

[env]
DEFMT_LOG = "info"
